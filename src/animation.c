#include "animation.h"
#include "raylib.h"


Animation LoadAnimation(const char *path, unsigned char frames, float time_between_frames)
{
    Image sprite_sheet =  LoadImage(path);
    if sprite_sheet.


}